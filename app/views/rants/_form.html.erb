<%= simple_form_for(rant) do |f| %>
  <%= f.input :title %>
  <%= f.input :gaijintag %>
  <%= f.input :content, :as => :text, :input_html => { 'rows' => 10} %>
  <div class="actions">
  <%= f.button :submit, :class => 'btn-primary' %>
  </div>
<% end %>
